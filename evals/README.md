# Evals

Behavioral regression tests, not campaign summaries.

Each eval records what the skill should recognize, what it must not overclaim, and what failure would indicate a regression. Expected results are directional rather than tied to one exact numeric score.
